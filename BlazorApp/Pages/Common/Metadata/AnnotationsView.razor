@using BlazorApp.Pages.Common.Html
@if (Annotations != null)
{
    <HTableColumn Title="Annotations">
        @foreach (var label in Annotations)
        {
            <Paragraph >
                <Text Code Copyable>
                    <span  >@label.Key = @label.Value</span>
                </Text>
            </Paragraph>
        }
    </HTableColumn>
}
