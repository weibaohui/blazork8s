@using BlazorApp.Pages.Common.Html
@using BlazorApp.Utils
@using k8s.Models
@if (Owner != null)
{
    <HTableColumn Title="ControllerBy">
        <ControllerBy
            ShowOwnerName="true"
            Refs="@(AutoMapperHelper<V1OwnerReference>.MapToObjectReference(Owner))">
        </ControllerBy>
    </HTableColumn>
}
