@using BlazorApp.Pages.Common.Html
@if (Owner != null)
{
    <HTableColumn Title=@L["ControllerBy"] ExplainField="@ExplainField">
        @foreach (var r in Refs)
        {
            <RefView Ref="r" FullView="true"></RefView>
        }
    </HTableColumn>
}
