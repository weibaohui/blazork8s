@using BlazorApp.Pages.Common.Html
@inherits PageBase

@if (Selector is { Keys.Count: > 0 })
{
    <HTableColumn Title="@Title" ExplainField="@ExplainField">
        @foreach (var label in Selector)
        {
            <ColorfulTag>@label.Key = @label.Value</ColorfulTag>
        }
    </HTableColumn>
}
