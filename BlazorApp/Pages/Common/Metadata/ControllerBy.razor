@if (Refs != null)
{
    @foreach (var r in Refs)
    {
        @if (ShowOwnerName)
        {
            <RefView Ref="@r" FullView="true" ></RefView>
        }
        else
        {
            <RefView Ref="@r"  ></RefView>
        }

    }
}
