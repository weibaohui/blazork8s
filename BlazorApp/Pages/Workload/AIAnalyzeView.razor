@using Extension
@inherits FeedbackComponent<Object, bool>

<Skeleton Active="true" Loading="@_loading">
    <Card >
        <Body>
        @_item.style
        @((MarkupString)Advice.ToHtmlDisplay())
        </Body>
    </Card>
</Skeleton>
