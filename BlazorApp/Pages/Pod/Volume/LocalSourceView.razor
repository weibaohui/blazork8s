@using k8s.Models
@using BlazorApp.Pages.Common.Metadata
@if (Local  != null)
{
    <PropertyOptionView Title="Type" Item="@Local.FsType" Options='@(new[] { "", "xfs", "ntfs", "ext4", "ext3", "ext2","btrfs","hpfs","vfat","xfs","jfs","reiserfs","swap" })' ExplainField=@(FieldPrefix+".hostPath.type")></PropertyOptionView>
    <PropertySimpleView Title="Path" ExplainField=@(FieldPrefix+".hostPath.path") Item="@Local.Path"></PropertySimpleView>
}


@code {

    [Parameter]
    public V1LocalVolumeSource Local { get; set; }

    [Parameter]
    public string FieldPrefix { get; set; }
}
