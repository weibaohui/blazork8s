@using k8s.Models
@using BlazorApp.Pages.Common.Html.HTable
@using BlazorApp.Pages.Common.Metadata
@using BlazorApp.Utils
@if (Volume.HostPath != null)
{
    <BadgeRibbon Text="@Volume.Name" Placement="start" Color="#832">
        <TextTemplate>
            <span style="color:#DCDCDC">@Index</span>
            <Icon Type="hdd" Theme="outline"/>
            @Volume.Name
        </TextTemplate>
        <ChildContent>
            <Card Bordered>
                <HTable >
                    <HTr>
                        <HTd>Volume</HTd>
                        <HTd >
                            <Tag>@Volume.Name</Tag> <Tag Color="@RandomColorHelper.RandomColor()">HostPath</Tag>
                            <KubectlExplainButton Field="pod.spec.volumes.hostPath"></KubectlExplainButton>
                        </HTd>
                    </HTr>

                    <PropertyOptionView Title="Type" Item="@Volume.HostPath.Type" Options='@(new[] { "", "DirectoryOrCreate", "Directory", "FileOrCreate", "File", "Socket", "CharDevice", "BlockDevice" })' ExplainField="pod.spec.volumes.hostPath.type"></PropertyOptionView>
                    <PropertySimpleView Title="Path" ExplainField="pod.spec.volumes.hostPath.path" Item="@Volume.HostPath.Path"></PropertySimpleView>
                </HTable>
            </Card>
        </ChildContent>
    </BadgeRibbon>
}


@code {

    [Parameter]
    public V1Volume Volume { get; set; }

    [Parameter]
    public int Index { get; set; }

}
