@if (Annotations != null)
{
    <DescriptionsItem Title="Annotations">
        @foreach (var label in Annotations)
        {
        <Paragraph Copyable>
            <Tag PresetColor=@PresetColor.Blue>@label.Key = @label.Value</Tag>
        </Paragraph>
        }
    </DescriptionsItem>
}
